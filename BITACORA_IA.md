# Bitácora de uso de IA

**Integrantes:** Laura Pabón, Lizeth Rodríguez  
**Herramientas usadas:** ChatGPT 4o, Claude 3.5 Sonnet, GitHub Copilot

> Las tres secciones son obligatorias. **`## Rechazado` es la que se califica.**  
> Una bitácora que solo lista prompts aceptados vale la mitad.

---

## Prompts

| # | Parte | Quién | Prompt |
|---|-------|--------|-------------------------------|
| 1 | A | Laura | «Ayuda a identificar defectos en un servicio FastAPI que funciona pero está mal hecho. Revisa este main.py y dime qué prácticas están mal según módulos de API: carga de modelo, códigos HTTP, serialización, decoradores, etc. Dame lista con síntoma, causa y cómo comprobar con curl. » |
| 2 | A | Lizeth | «Para cada defecto que encuentres, dime qué módulo y sección del material de Python para APIs e IA (módulos 1-5) lo respalda. Cita exacta: "Módulo X Sección Y"» |
| 3 | B | Laura | «Necesito refactorizar este servicio FastAPI para que cumpla: (1) modelo cargado al inicio, (2) validación con Pydantic BaseModel + Field, (3) HTTP 422 para errores de validación, (4) endpoint /health, (5) arranque de producción sin --reload. Dame el có­digo corregido. » |
| 4 | B | Lizeth | «Escribe un decorador en Python que registre llamadas a una función pero que no oculte la identidad de la función original (que se pueda ver el nombre con .__name__) y que no trague excepciones devolviendo None. » |
| 5 | C | Laura | «Tengo 4 endpoints: /ping (trivial), /consulta-archivo (lee disco), /servicio-externo (llama HTTP externo), /calculo-pesado (cálculo numérico intensivo). Clasifícarlos como IO-bound, CPU-bound o trivial, y dime si deben ser def, async def o async def + executor. Justifica. » |
| 6 | C | Lizeth | «La IA me dijo que todo I/O debe ser async def, pero midiendo /consulta-archivo con async def y con def da igual. ¿Por qué puede pasar esto? ¿Cuándo NO conviene usar async para I/O? » |
| 7 | D | Laura | «Revisa este ia_propuesta.py generado por IA. Encuentra 3 defectos de comportamiento (no de sintaxis) que se puedan demostrar ejecutando có­digo. Para cada uno: qué está mal, por qué es defecto, cómo comprobarlo, y corrección. » |
| 8 | D | Lizeth | «En Pydantic v2, ¿un Field(pattern=...) valida automáticamente un email? Si no, ¿cómo se hace correctamente? » |
| 9 | E | Ambas | «Dame ejemplos de qué poner en una bitácora de IA para un taller universitario: prompts, qué aceptamos, qué rechazamos y por qué. » |

---

## Aceptado

| # | Qué propuso la IA | Por qué lo aceptamos | Qué cambiamos antes de usarlo |
|---|-------------------|----------------------|-------------------------------|
| 1 | Fijar versiones de `requirements.txt` con `pip freeze > requirements.txt` | Es el mecanismo estándar visto en el material del curso (Módulo 1) para garantizar reproducibilidad. Sin versiones fijadas, `pip install` resuelve siempre la versión más reciente, que varía según cuándo y dó­nde se instale. | Se generó el archivo en la máquina local de Laura, no se copiaron las versiones que la IA tenía en su propio entorno de prueba, porque diferían (numpy, scikit-learn). |
| 2 | Lista de 12 posibles defectos en el servicio (H1-H12), incluyendo: modelo cargado en cada petición, falta de /health, uso de pickle en /exportar, decorador que traga excepciones, README con --reload para producción. | La lista coincidió con lo que encontramos al revisar el có­digo manualmente. Los comandos curl eran ejecutables y reproducibles. | Descartamos 3 defectos que no pudimos comprobar (H11 y H12 eran dudosos). Ajustamos los comandos para que funcionaran en nuestro entorno (puerto 8000). |
| 3 | Citas a mó­dulos: M2 · 2. El protocolo HTTP y la autenticación (para códigos de estado), M5. 8. Resumen y mejores prá­cticas (para carga de modelo), M1. Mó­dulo 5: Decoradores Estadísticos (para el decorador `con_registro`). | Las citas corresponden a temas que sí vimos en clase y el contenido coincide con lo explicado en las diapositivas. | Verificamos que los tí­tulos de sección existieran en el material del curso. Cambiamos "Módulo 5" por "M1. Mó­dulo 5" para que el formato coincidiera con el menú lateral. |
| 4 | Mover el historial de evaluaciones a una clase `RepositorioEvaluaciones` separada de `EvaluadorRiesgo`, inyectada como colaborador opcional | Resuelve el test `test_el_historial_no_se_comparte_entre_instancias` sin romper el contrato de que `EvaluadorRiesgo` se construye solo con la pó­liza. Compartir estado mutable de clase es anti-patrón en POO. | Ninguno; se aplicó tal como se propuso. El repositorio por defecto es compartido, pero la responsabilidad de guardar vive en el repositorio, no en el evaluador. |
| 5 | Có­digo refactorizado con: `MODELO = pickle.load(...)` a nivel de mó­dulo, `@app.get("/health")`, `ScorePayload` con `BaseModel` y `Field(gt=0)`, `raise HTTPException(status_code=422)` para validación. | El có­digo generaba exactamente las restricciones B1-B9 del enunciado. Los tests de `tests/test_contrato.py` pasaron después de aplicar los cambios. | Añádimos el validador de campo `@field_validator("poliza")` que la IA no había incluido inicialmente. Separamos el repositorio de evaluaciones en una clase aparte (`RepositorioEvaluaciones`) para mejor POO. |
| 6 | Decorador con `@functools.wraps(func)` para conservar `__name__` y `__doc__`, y `raise` en vez de `return None` para propagar excepciones después de registrar. | Cumplía con B9: "Ningún decorador propio oculta la identidad de la función que envuelve, ni captura excepciones para devolver None". | Mantuvimos el `print(f"[registro] {func.__name__} falló·¿·: {exc}")` para que el registro siguiera ocurriendo, pero cambiamos `return None` por `raise` para que el error no se tragara. |
| 7 | Clasificación: /ping = trivial → `def`; /consulta-archivo = IO-bound → `async def`; /servicio-externo = IO-bound → `async def`; /calculo-pesado = CPU-bound → `async def + executor`. | Coincidía con la teoría de concurrencia asíncrona: operaciones triviales no necesitan async, I/O se beneficia de async, CPU-bound necesita executor. | Después de medir (Parte C), nos dimos cuenta de que /consulta-archivo con `async def` no mejoraba respecto a `def` porque `Path.read_text()` es síncrono. Decidimos mantener `async def` por consistencia, pero lo documentamos en HALLAZGOS.md. |
| 8 | Tres defectos en `ia_propuesta.py`: (1) validador sin `return`, (2) `time.sleep()` en `async def`, (3) `pattern` en `Field()` no valida en Pydantic v2. | Los tres se podí£¬an demostrar con có­digo ejecutable. Las pruebas que propuso la IA funcionaban en nuestro entorno. | Añádimos un test automático en `ia_propuesta_corregida.py` que verifica que las tres correcciones funcionan (asserts con tiempo de ejecución y validación de email). |

---

## Rechazado

| # | Qué propuso la IA | Por qué lo rechazamos | Qué hicimos en su lugar |
|---|-------------------|-----------------------|-------------------------|
| 1 | Citar `M1 · Mó­dulo 2: Entornos Virtuales` como justificación del hallazgo sobre secretos hardcodeados en `config.py` | Al verificar el contenido real del material del curso, esa sección habla de entornos virtuales, no de secretos ni `.gitignore`; el contenido sobre eso vive en otra sección. Una cita inventada anula la fila. | Se buscó y confirmó la cita correcta: `M1 · Mó­dulo 4: Git y GitHub para Investigadores`. Verificamos que el tí­tulo existiera en el menú lateral del material. |
| 2 | Validar monto negativo con `assert`: `assert payload["monto"] > 0, "monto debe ser positivo"` dentro del handler `/score` | `assert` lanza `AssertionError` que FastAPI convierte en HTTP 500, no 422. Además, `assert` se desactiva con `python -O`, lo que haría que la validación desapareciera en producción. Esto incumplía B2 y B5. | Usamos `Field(gt=0)` en `ScorePayload` para que Pydantic validara automá­ticamente y devolviera 422. Esto es declarativo, no depende de `assert`, y FastAPI responde 422 sin có­digo extra. |
| 3 | Corregir el `assert` de validación de `/score` con un `raise HTTPException(422, ...)` puntual dentro del handler | Al implementar B5 (validación con Pydantic) de todos modos, ese `raise` manual quedaba redundante: FastAPI ya valida automáticamente con el esquema `ScorePayload`. | Se dejó que la validación de Pydantic maneje el caso completo, sin có­digo manual adicional. `Field(gt=0)` en `monto` es declarativo y responde 422 automáticamente. |
| 4 | Cargar el modelo dentro del handler `/score`: `with open("modelo.pkl", "rb") as fh: modelo = pickle.load(fh)` dentro de la función `score()` | Esto incumplía B6: "El modelo se carga al iniciar el servicio, no dentro del handler". Cargar en cada petición es ineficiente (I/O repetido) y puede causar inconsistencia si el modelo cambia entre peticiones. | Movimos la carga a nivel de mó­dulo: `MODELO = pickle.load(...)` fuera de cualquier handler. Así se carga una sola vez al iniciar el servicio (al importar `main.py`). |
| 5 | Quitar por completo el `try/except` del decorador `con_registro` para que no oculte errores | Eliminarlo por completo también quitaba el registro (`print`) de la falla, que sí era Ú­til para depuración. El decorador debe registrar, pero no tragar la excepción. | Se conservó el `try/except`, pero se cambió `return None` por `raise` (registra y luego propaga). Se añ­adió `@functools.wraps(func)` para conservar la identidad de la función original. |
| 6 | Incluir el valor real de `API_KEY` y `CLAVE_FIRMA` como ejemplo literal en la sección de instalación del `README.md` | Habría vuelto a exponer los secretos en el historial de git, el mismo problema que se corrigió en `config.py` (hallazgo H3). Laura detectó esto al revisar el README antes de subirlo. | Se reemplazó por valores de ejemplo gené­ricos (`"tu-clave-aquí£¬"`), indicando que los valores reales se piden al equipo. Los secretos reales van en variables de entorno, no en el có­digo. |
| 7 | Usar `async def` para todos los endpoints: "es mejor prá­ctica en FastAPI" y "siempre da mejor rendimiento" | La Parte C del taller dice explicitamente: "la regla 'I/O va con async' falla en má­­s de un caso". Midimos y /ping con `async def` era má­­s lento que con `def` (overhead del event loop). Además, /consulta-archivo con `async def` no mejoraba porque `read_text()` es síncrono. | Clasificamos cada endpoint: /ping = trivial → `def`; /consulta-archivo = IO-bound → `async def` (pero documentamos que no hay mejora); /servicio-externo = IO-bound → `async def` con `await asyncio.sleep()`; /calculo-pesado = CPU-bound → `async def + executor`. Decidimos después de medir, no antes. |
| 8 | Implementar de inmediato el cambio de `time.sleep()` a `await asyncio.sleep()` en `/servicio-externo`, y un `ProcessPoolExecutor` para `/calculo-pesado` | Laura decidió mantener el có­digo de los 4 endpoints de carga sin modificar por ahora, documentando la clasificación / decisión / justificació en `HALLAZGOS.md` sin implementar el cambio todavía. El enunciado permite modificar, pero no obliga. | Se dejó el có­digo de los 4 endpoints tal como estaba en `v0-semilla`, con la corrección propuesta documentada pero no aplicada. Las mediciones de la Parte C se hicieron sobre el có­digo original. |
| 9 | Serializar con pickle en /exportar: `return pickle.dumps(datos)` para "ahorrar tiempo" y porque "es má­­s rápido que JSON" | Esto incumplí£¬a B3: "Nada se serializa con pickle hacia el cliente". Pickle es inseguro (puede ejecutar có­digo arbitrario al deserializar) y no es interoperable (clientes no-Python no lo leen). | Devolvemos los datos directamente: `return cargar_siniestros()`. FastAPI los serializa a JSON automá­ticamente con `Content-Type: application/json`. |
| 10 | Documentar `--reload` para producción: `uvicorn main:app --reload --host 0.0.0.0 --port 8000` | `--reload` es para desarrollo: vigila cambios en disco y reinicia el servidor, lo cual añade sobrecarga y comportamiento no determinista. En producción, los cambios se despliegan con CI/CD. Incumplía B8. | Documentamos dos comandos: desarrollo (`--reload`) y producción (`--workers 4` sin `--reload`). Aclar explicitamente que `--reload` no es apto para producción. |
| 11 | Devolver `{"error": "mensaje"}` con HTTP 200: `return {"error": "faltan campos"}` con estado 200 | Esto incumplía B2: "Los có­digos de estado HTTP son correctos y el error viaja en el estado, no en el cuerpo con 200". Un error de validación debe ser 422, un recurso no encontrado debe ser 404. | Usamos `raise HTTPException(status_code=422, detail="...")` para errores de validación y `raise HTTPException(status_code=404, detail="...")` para recursos no encontrados. |
| 12 | Compartir historial entre instancias de `EvaluadorRiesgo`: `historial = []` como atributo de clase | Esto fallaba el test `test_el_historial_no_se_comparte_entre_instancias`: si `a` y `b` son dos evaluadores, `b` no debe ver las anotaciones de `a`. Compartir estado mutable de clase es anti-patrón en POO. | Movimos el historial a un colaborador aparte: `RepositorioEvaluaciones`. Cada `EvaluadorRiesgo` recibe una referencia opcional al repositorio, y por defecto usan el mismo, pero la responsabilidad de guardar vive en el repositorio, no en el evaluador. |

---

## Lecciones aprendidas

1. **Medir antes de decidir (Parte C):** La IA nos dijo "usa async para todo", pero las mediciones mostraron que no siempre mejora. La regla "I/O → async" tiene excepciones (ej. `read_text()` es síncrono).
2. **Validación declarativa > assert (Parte B):** `Field(gt=0)` es má­­s seguro y claro que `assert`. Además, FastAPI responde 422 automáticamente, sin có­digo extra.
3. **Pydantic v2 es distinto a v1:** `Field(pattern=...)` ya no valida en v2. Hay que usar `@field_validator` o `Annotated[str, StringConstraints(pattern=...)]`.
4. **El decorador debe conservar la identidad:** `@functools.wraps(func)` no es opcional si queremos que `func.__name__` y `func.__doc__` se preserven. Además, no se debe tragar excepciones con `return None`.
5. **No exponer secretos en el README:** Aunque la IA lo sugirió como "ejemplo", exponer `API_KEY` y `CLAVE_FIRMA` en el README los sube al historial de git. Mejor usar valores genéricos (`"tu-clave-aquí£¬"`) y variables de entorno.
6. **La bitácora no es solo lista de prompts:** Lo que se califica es mostrar criterio: qué rechazamos y por qué. Usar IA no es aceptar todo ciegamente; hay que verificar contra el enunciado y el material del curso.
